В Python функция map принимает два аргумента: функцию и аргумент составного (итерируемого объекта) типа данных,
например, список.
map применяет к каждому элементу списка переданную функцию. Например, вы прочитали из файла список чисел,
изначально все эти числа имеют строковый тип данных, чтобы работать с ними - нужно превратить их в целое число:

map(f, *args)

map возвращает итератор, а не список

Чтобы выполнить второй проход, необходимо  снова создать итератор

В контексте итераций функция next() вызывается автоматически


