a - добавление записи
r - чтение
w - запись
+ - чтение и запись
wb - режим записи байтов
rb - режим чтения байтов

open('filename', 'w', encoding = 'utf-8')

Запись
write - записать строку в файл
writelines - записать список строк в файл
\n перенос строки

Чтение

read  - чтение файла в одну строку
for line in f:  -  чтение построчно
readlines() - чтение файла в список строк

закрыте файла

f.close()
with

ТИПЫ СТРОК В Python:

str - обычные строки
bytes - строки байт
bytearray - изменяемая строка байт

Действия со строками типа байт

индекс sb[1]
срез sb[1:3]


Кодировки

ascii - американские символы
latin - добавляет европейские символы
UTF-8 - универсальная кодировка для большинства языков

перевод строки в байты

'Hello, World!'.encode('UTF-8')

Декодирование

sb.decode(UTF-8)

Запись байтов в файл

f.write(b'some bytes') - файл открыт в режиме wb
f.write('some str') - файл открыт в режиме w

Чтение  байтов из файла

f.read() - файл открыт в режиме rb - читаем байты
f.read() - файл открыт в режиме r -  читаем строки

СЕРИАЛИЗАЦИЯ

процесс преобразования обхекта в поток байтов для сохранения или передачи в пямять, базу данных
или файл

обратный процесс - десериализайия

1.применение: сохранение обекта в файл
2.сохранение объекта и базу данных
3.передача объекта по сети

Способы записи объекта в файл:

1. ручной "создание велосипеда"
2. универсальный pickle

Основные функции модуля Pickle

dump - сохраниние объекта
dumps - преобразование объекта
load - загрузка объекта из файла
loads - загрузка объекта из на бора байт






