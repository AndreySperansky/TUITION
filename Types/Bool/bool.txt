Истина = 'abc', [1], (1,), {1:'a'}, 10, 1.1, ...
Ложь = '', [], (), {}, 0, None,
(1,) - кортеж

вместо if len(str_var)> 0:
пишем: if str_var:

if s:
	print('Строка пустая')
else:
	print('Строка пустая')